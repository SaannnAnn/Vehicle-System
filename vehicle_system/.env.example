# Django
SECRET_KEY=django-insecure-replace-this-with-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# Leave DATABASE_URL unset to use local SQLite (good for quick setup/testing).
# For Postgres, uncomment and set your own dummy/sample values:
# DATABASE_URL=postgres://vehicle_user:vehicle_password@localhost:5432/vehicle_system_db
